import os
import sys

purchaes = {}
prev_city = None

for line in sys.stdin:
    line = line.strip()
	line = line.split('\t')
	if line[0] == prev_city:
	    purchases[str(prev_city)].append(float(line[1]))
	else:
	    prev_city = line[0]
		purchases[prev_city] = [float(line[1])]
		
print(purchases)